<project default="compile">

    <path id="classpath">
        <fileset dir="lib" includes="**/*.jar"/>
    </path>

    <target name="compile">
        <javac srcdir="." classpathref="classpath" includes="ndg-commons-core/**/*.java"/>
    </target>

</project>
